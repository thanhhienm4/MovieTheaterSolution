
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@for(int i=0;i<7;i++)
{
    DateTime date = @DateTime.Now.AddDays(i);
        <button class="btn bg-light" data-date="@date">@date.Month/@date.Day</button>
}

